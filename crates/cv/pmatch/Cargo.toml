[package]
name = "pmatch"
version = "0.1.0"
edition = "2021"

[dependencies]
image = { workspace = true }
rayon = { workspace = true }

[dev-dependencies]
image = { version = "0.25", default-features = false, features = ["jpeg"] }
